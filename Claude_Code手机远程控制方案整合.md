# Claude Code 手机端 Agent 消息接收与功能改进参考

## 一、文档说明

本文档整合自小红书上的相关讨论。由于你（Claude Code）无法直接访问小红书查看内容，我把这些内容整合到了一起，方便你参考。文档中涉及的相关 GitHub 仓库链接和其他网址链接，都已贴在对应位置。

## 二、当前项目现状

当前核心用途是：**手机端接收电脑端 Agent 状态消息**。

目前手机端还没有完全解决接收电脑端 Agent 消息状态的问题，还存在一些问题。希望你（Claude Code）在解决这个接收问题的基础上，参考本文档内容做相关改进。

## 三、改进方向

1. **解决手机端接收电脑端 Agent 消息状态的问题**：这是当前的基础问题，先解决消息接收。
2. **像电脑端 Claude Code 那样使用，拥有 memory 记忆能力**：希望手机端能具备类似电脑端 Claude Code 的 memory 功能。
3. **前端美化参考原生 Claude APP**：界面风格和交互参考官方 Claude APP 的设计。
4. **参考文档中整合的思路和 GitHub 仓库链接**：下文整理了小红书上看到的方案和社区开源项目，可参考这些已有思路。
5. **你（Claude Code）也可以自己上网检索前人的经验或有用的网址链接作为参考**，不局限于本文档内容。

## 四、核心背景

Anthropic 官方现已为 Claude Code 推出 **Remote Control（远程控制）** 功能，对标 OpenClaw 的核心能力（远程控制、心跳机制、长期记忆），可以实现**手机/平板浏览器远程操控本地电脑上的 Claude Code**，随时随地 Vibe Coding；同时社区也诞生了多款开源替代方案，下文区分**官方原生方案**、**社区第三方方案**两类整理。

> Vibe Coding：把编码当成创作，不需要一直坐在电脑前，在通勤、散步、休息时发送指令，本地电脑完成代码执行，移动端只做消息收发、查看结果。

## 五、Anthropic 官方 Remote Control（原生方案）

### 使用前提
1. 权限限制：**仅 Claude Max 会员可用，Pro 会员暂无法使用**，属于研究预览版，后续会开放给 Pro 用户；
2. 运行环境：电脑本地安装 Claude Code 终端客户端。

### 操作步骤
1. 在电脑端 Claude Code 终端，输入命令启动远程控制：
   ```
   /remote-control
   ```
2. 执行后自动生成专属会话链接，格式：`https://claude.ai/code/session_xxxxxx`
3. 在手机/平板浏览器打开该链接，**浏览器必须登录你自己同一个 Claude 账号**，账号 + 临时会话链接双重校验，才能接入会话。

### 核心原理与安全机制
1. **只出不进，不监听入站端口**
   本地 Claude Code 只会主动向外轮询 Anthropic 服务器查询新指令，**不会在电脑上打开可被外部主动访问的端口**。类比：家门全程从内部锁死，本地程序定时开门向外看有没有任务，外部无法主动敲门接入你的电脑，不存在传统远程工具的端口暴露后门风险。
2. **双重安全校验**
   - 临时一次性会话链接（单次会话有效，用完作废）
   - 账号强绑定：就算链接泄露，访问者必须登录**你的 Claude 账号**才能连接；仅有链接、账号不匹配，完全无法控制电脑。
3. 传输：全部流量走 TLS 加密传输，和普通 Claude Code 会话安全等级一致。

### 进阶能力
1. **多终端并行**：电脑打开多个独立终端，多次执行 `/remote-control`，生成多条独立链接；手机浏览器多标签页打开，实现多会话并行工作。
2. **自动免确认模式（跳过权限弹窗）**
   Vibe Coding 痛点：频繁弹窗确认指令打断工作流，可以启动时开启绕过权限校验，自动执行指令。启动命令：
   ```
   claude --dangerously-skip-permissions
   ```
   > ⚠️ 风险提示：该模式会跳过所有权限检查，允许 Claude 直接执行各类操作，存在文件丢失、系统修改风险，官方建议仅在沙箱容器内使用；开启成功后终端提示栏会显示 `bypass permissions on`。
3. 数据互通：文件系统、MCP 服务器、Skills 插件、项目配置全部沿用本地电脑环境；所有设备会话双向同步，终端/网页/手机可以交替发指令，由本地终端统一执行。
4. 配套新增能力：Cowork 定时任务，支持设置定时自动执行重复任务（周报、表格更新、团队汇报等）。

## 六、社区第三方开源方案（官方 Remote Control 出现前的替代方案）

### 方案 1：happy-coder
- **官网：** https://happy.engineering/
- **原理：** 把内网穿透、终端连接能力封装整合，一键打通手机 ↔ 本地电脑 Claude Code。
- **操作流程：**
  1. 手机端下载对应 APP；
  2. 在电脑终端执行指令安装 happy coder，`happy` 命令启动服务；
  3. 手机 APP 扫码，完成电脑与手机配对连接；
  4. 连接成功，手机发送指令，本地电脑执行 Claude Code 任务，适合外出远程处理电脑上项目临时工作。

### 方案 2：Tailscale + Termius（传统手动内网穿透方案）
- **原理：** Tailscale 做内网穿透，Termius 作为手机端终端模拟器，手机直接 ssh 远程登录电脑终端，手动调用 Claude Code。
- **缺点：** 需要手动配置网络，部署繁琐，对比 happy-coder 上手成本更高。

### 方案 3：myrlin-workbook
- **Github 仓库：** https://github.com/therealarthur/myrlin-workbook
- 社区开源项目，可用于手机端对接 Claude Code 远程编码。

### 其他社区玩法
- OpenClaw 对接 Claude Code、飞书接入 Claude Code 远程会话；这类属于二次封装方案，存在一定稳定性与安全隐患。

## 七、方案对比小结

| 维度 | 官方 Remote Control | 社区开源方案（happy-coder 等） |
| --- | --- | --- |
| 优点 | 原生支持、部署极简、安全模型完善；本地项目配置/MCP 插件完全复用，会话多端同步 | 不受会员套餐限制 |
| 缺点 | **仅限 Max 会员**，有会员门槛 | 社区二次开发项目，稳定性、安全模型不如官方原生方案，存在额外依赖维护成本 |

## 八、补充场景描述

目标场景：外出通勤、散步、休假、出门走亲戚时，不用坐在电脑前，使用手机随时下发编码指令，查看 Claude Code 执行结果，远程管理电脑上的代码项目；手机仅作为输入输出的信号接收器，**全部代码执行、文件读写都在本地电脑运行**。

## 九、参考链接汇总

| 链接 | 说明 |
| --- | --- |
| https://github.com/xinwithyu/dwell-on-something | 本人项目仓库（改进设计的起点） |
| https://happy.engineering/ | happy-coder 官网（社区第三方方案） |
| https://github.com/therealarthur/myrlin-workbook | myrlin-workbook GitHub 仓库（社区开源项目） |
| https://claude.ai/code/session_xxxxxx | 官方 Remote Control 会话链接格式（示例） |
